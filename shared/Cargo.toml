[package]
name = "shared"
version = "0.1.0"
edition = "2024"

[dependencies]
actix-web = { version = "4.13", optional = true }
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[features]
actix = ["actix-web"]
